openapi: 3.0.3
info:
  title: Ping API
  version: 1.0.0
paths:
  /ping:
    get:
      summary: Vérifie si l'API répond
      responses:
        '200':
          description: Réponse réussie
          content:
            text/plain:
              schema:
                type: string
                example: pong